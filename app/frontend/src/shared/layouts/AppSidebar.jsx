import React from "react";
import { Link } from "react-router-dom";
import NavSection from "./NavSection";

export default function AppSidebar({ brandIcon: BrandIcon, brandTitle, brandSubtitle, sections, footer, user, onClose, tone = "app", collapsed = false }) {
  return <aside className={`${collapsed ? "w-20" : "w-72"} shrink-0 flex flex-col border-r border-border bg-[#FBF6EF]/80 backdrop-blur transition-[width] duration-200`}><Link to="/" className={`h-16 ${collapsed ? "justify-center px-3" : "px-5"} flex items-center gap-2.5 border-b border-border`} title={collapsed ? `${brandTitle} ${brandSubtitle}` : undefined}><div className={`h-9 w-9 rounded-full ${tone === "admin" ? "bg-dusk-700" : "bg-clay-500"} grid shrink-0 place-items-center`}>{BrandIcon && <BrandIcon className="h-4 w-4 text-white" />}</div>{!collapsed && <div className="min-w-0"><div className="truncate font-heading font-semibold text-[15px]">{brandTitle}</div><div className="truncate text-[10px] uppercase tracking-[0.22em] text-muted-foreground">{brandSubtitle}</div></div>}</Link><nav className={`${collapsed ? "p-3" : "p-4"} flex-1 overflow-y-auto space-y-5`}>{sections.map((s) => <NavSection key={s.label} label={s.label} items={s.items} onItemClick={onClose} tone={tone} collapsed={collapsed} />)}{!collapsed && footer?.adminLink && (user?.role === "admin" || user?.role === "super_admin") && footer.adminLink}</nav>{!collapsed && footer?.bottom && <div className="p-4 border-t border-border">{footer.bottom}</div>}</aside>;
}

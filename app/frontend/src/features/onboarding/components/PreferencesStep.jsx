import React from "react";
import { CheckCircle2 } from "lucide-react";
import { CheckboxField, SelectField } from "../../../shared/ui";
import { EXAM_FAMILY_OPTIONS, INDIAN_STATE_OPTIONS, SECTOR_OPTIONS } from "../../../lib/profileFields";

function Chip({ active, onClick, children }) { return <button type="button" onClick={onClick} className={`text-left p-3 rounded-xl border-2 transition ${active ? "border-clay-500 bg-clay-50" : "border-border hover:border-clay-300"}`}><div className="flex items-center justify-between"><div className="font-semibold">{children}</div>{active && <CheckCircle2 className="h-4 w-4 text-sage-600" />}</div></button>; }

export default function PreferencesStep({ form, set, toggleArray, errors }) { return <div className="space-y-5"><div><div className="text-xs mb-2 text-muted-foreground">Target exam families</div><div className="grid md:grid-cols-2 gap-3">{EXAM_FAMILY_OPTIONS.map((e) => <Chip key={e} active={form.goal_exams.includes(e)} onClick={() => toggleArray("goal_exams", e)}>{e.replaceAll("_", " ")}</Chip>)}</div></div><div><div className="text-xs mb-2 text-muted-foreground">Preferred sectors</div><div className="grid md:grid-cols-2 gap-3">{SECTOR_OPTIONS.map((s) => <Chip key={s} active={form.preferred_sectors.includes(s)} onClick={() => toggleArray("preferred_sectors", s)}>{s.replaceAll("_", " ")}</Chip>)}</div></div>{errors.preferences && <div className="text-xs text-destructive">{errors.preferences}</div>}<SelectField label="Preferred states" value="" onChange={(e) => e.target.value && toggleArray("preferred_states", e.target.value, 6)}><option value="">Select state to add</option>{INDIAN_STATE_OPTIONS.map((s) => <option key={s} value={s}>{s.replaceAll("_", " ")}</option>)}</SelectField><div className="text-xs text-muted-foreground">Selected: {form.preferred_states.length ? form.preferred_states.map((s) => s.replaceAll("_", " ")).join(", ") : "Not provided"}</div><CheckboxField label="Willing to relocate" checked={form.willing_to_relocate} onChange={(e) => set("willing_to_relocate", e.target.checked)} /></div>; }

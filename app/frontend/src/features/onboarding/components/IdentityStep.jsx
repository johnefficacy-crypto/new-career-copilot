import React from "react";
import { InputField, SelectField } from "../../../shared/ui";
import { CATEGORY_OPTIONS, GENDER_OPTIONS, INDIAN_STATE_OPTIONS, PWBD_OPTIONS } from "../../../lib/profileFields";

export default function IdentityStep({ form, set, errors }) {
  return <div className="grid md:grid-cols-2 gap-4"><InputField label="Full name" required value={form.name} onChange={(e) => set("name", e.target.value)} error={errors.name} /><InputField label="Date of birth" required value={form.date_of_birth} onChange={(e) => set("date_of_birth", e.target.value)} error={errors.date_of_birth} placeholder="YYYY-MM-DD" /><SelectField label="Gender" required value={form.gender} onChange={(e) => set("gender", e.target.value)} error={errors.gender}>{GENDER_OPTIONS.map((g) => <option key={g.value} value={g.value}>{g.label}</option>)}</SelectField><SelectField label="Category" required value={form.category} onChange={(e) => set("category", e.target.value)} error={errors.category}><option value="">Not provided</option>{CATEGORY_OPTIONS.map((c) => <option key={c} value={c}>{c}</option>)}</SelectField><SelectField label="PwBD status" value={form.pwbd_status} onChange={(e) => set("pwbd_status", e.target.value)}><option value="">Not provided</option>{PWBD_OPTIONS.map((p) => <option key={p} value={p}>{p}</option>)}</SelectField><SelectField label="Domicile state" required value={form.state} onChange={(e) => set("state", e.target.value)} error={errors.state}><option value="">Not provided</option>{INDIAN_STATE_OPTIONS.map((s) => <option key={s} value={s}>{s.replaceAll("_", " ")}</option>)}</SelectField></div>;
}
